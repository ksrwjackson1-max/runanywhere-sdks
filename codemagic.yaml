workflows:
  android-agent:
    name: Android Use Agent Build
    scripts:
      - name: Build Debug APK
        script: |
          cd Playground/android-use-agent
          ./gradlew clean assembleDebug
    artifacts:
      - Playground/android-use-agent/app/build/outputs/**/*.apk